name: DEPLOY TO STAGING
on:
  push:
    branches:
    - main
    - master
jobs:
  linting:
    runs-on: ubuntu-latest
    steps:
    - run: echo "Linting"
  testing:
    runs-on: ubuntu-latest
    needs: linting
    steps:
    - run: echo "Integration testing"
  deploy-staging:
    runs-on: ubuntu-latest
    steps:
    - run: echo "DEPLOY TO STAGING"